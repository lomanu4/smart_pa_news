import 'package:flutter/material.dart';

class DividerDetails extends StatelessWidget {
  const DividerDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Color.fromARGB(255, 170, 162, 162),
      height: 5,
    );
  }
}
